#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>
#include <vector>
#include <cmath>
#include <algorithm>

class utils {
    public:
        int findVectorIndex(const std::vector<int> inputVector, int searchVal);
};

#endif